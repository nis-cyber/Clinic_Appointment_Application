import 'package:flutter/material.dart';

class AppointmentRemindersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Appointment Reminders'),
      ),
      body: Center(
        child: Text('Appointment Reminders Screen'),
      ),
    );
  }
}
