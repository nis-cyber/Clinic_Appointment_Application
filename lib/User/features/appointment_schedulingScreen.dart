import 'package:flutter/material.dart';

class AppointmentSchedulingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Appointment Scheduling'),
      ),
      body: Center(
        child: Text('Appointment Scheduling Screen'),
      ),
    );
  }
}
